#!/bin/bash

etcdctl --endpoints=http://{{ MASTER_IP }}:{{ etcd_port_c1 }} set {{ FLANNEL_ETCD_PREFIX }}/config '{"Network":"'{{ pod_cidr }}'", "SubnetLen": 24, "Backend": {"Type": "{{ FLANNEL_BACKEND }}"}}'