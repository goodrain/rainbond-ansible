#!/bin/bash

if [ -d "{{ ca_dir }}" ];then
    rm -rf {{ ca_dir }}/*
fi;
if [ -d "{{ region_ca_dir }}" ];then
    rm -rf {{ region_ca_dir }}/*
fi;
if [ -d "{{ kubecfg_dir }}" ];then
    rm -rf {{ kubecfg_dir }}/*
fi;
if [ -d "/grdata/kubernetes/" ];then
    rm -rf /grdata/kubernetes/
fi;
