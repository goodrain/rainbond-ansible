#!/bin/sh

exec /usr/bin/docker \
  run \
  --privileged \
  --restart=always \
  --net=host \
  --name etcd-proxy \
  goodrain.me/{{ etcd_image }} \
  /usr/local/bin/etcd \
  grpc-proxy start \
  --endpoints=${ETCD_MASTER} \
  --listen-addr=127.0.0.1:2379